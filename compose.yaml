services:
  backend:
    build: ./backend
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_SERVER_WS_URL: /ws
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "${PORT:-8080}:80"